package com.determinasian.outandaboutevents.navigation

object Graph { // graph labels
    object Routes {
        const val ROOT = "root_graph"
        const val APP = "app_graph"
        const val LIST = "list_graph"
    }
}
